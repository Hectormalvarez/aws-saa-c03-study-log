# Local state only
*.session
__pycache__/
*.pyc
.venv/
.DS_Store

# Everything study-related is committed — the repo is the system of record:
# prescriptions, answers, mocks, session log, attempts all stay in git.
