# Prescription — Day 1 · Tue 2026-09-15 · 13 days to exam · phase: first pass

## Execution check (vs prior prescription)
- **Prior prescription: none** — day 0 was repo setup. Nothing rolled forward.
- Morning `daily.py` ran 07:19: cards 8/18 (44%), scenarios 5/6 (83%) → **54% overall**.
- ⚠ Session evidence landed inside `docs(sprint)` commit instead of the standard
  `study(day N)` message. Data is committed; format corrected from today forward.

## Midday (~10 min) — requeue clear
- **10 cards due now** — run `python3 scripts/quiz.py --due`. Clear every same-day
  requeue; do not carry them to tomorrow.

## Tonight's priorities (mechanical miss clusters)
1. **D3 High-Performing = weakest domain (4 requeues) → force-drill it first:**
   - EBS gp3 vs gp2 upgrade math (`hp-001`)
   - Lambda hard limits (`hp-002`)
   - DynamoDB on-demand vs provisioned — when each
   - Aurora read replica limits vs RDS (`hp-005`)
2. Requeues, other domains: STS AssumeRole vs WebIdentity (`sec-003`),
   Secrets Manager vs Parameter Store, SQS/SNS/EventBridge one-liners,
   ASG target/step/scheduled scaling, S3 lifecycle minimum-days.
3. **Scenario miss `res-q4`** (resilient) — re-do from `domains/02-resilient/questions.json`
   until you can state the rule behind the answer, not just the answer.

## New material (17:30–19:30, retrieval-first)
- First pass continues with the two highest-weight domains: **D1 Secure (30%)** and
  **D2 Resilient (26%)** — next unworked checklist items. Attempt cold recall of the
  item definition BEFORE reading the cheat sheet.

## What to author (20:00 block → Content Author, vet gate)
- 5–10 scenario questions against tonight's new D1/D2 topics. Vet every answer
  yourself before anything enters the decks — wrong answers poison the scheduler.

## Cold-verifiable tonight (tick ONLY on unprompted recall)
- D3 3.1: EBS volume types gp2 vs gp3 (IOPS vs throughput)
- D3 3.2: Lambda limits + concurrency
- D3 3.4: Aurora up to 15 replicas; RDS Multi-AZ = availability, NOT perf
- D3 3.4: DynamoDB on-demand vs provisioned + hot partitions

## Design answer (from memory, written)
- One answer to today's drill prompt → `practice/answers/2026-09-15.md`.

## Mock status
- **Not due.** Next mock: **day 4 = Fri 2026-09-18** (Udemy practice tests, ≥65% on-track bar).

## Close-out
- ~21:00: `python3 scripts/daily.py --micro` then sleep — consolidation beats cramming.
